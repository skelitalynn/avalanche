# T002 设计就绪验收

日期：2026-09-19。工作目录：`/tmp/avalanche-t002`。关联 [PR #4](https://github.com/skelitalynn/avalanche/pull/4)。这是 Spec 设计验收，不是实现或真实链上验收。

## 决策证据

项目发起人已明确回复：“确认，你直接用你推荐的方案就好”。采用 D1（48h 说明、72h 投票、否决/超时各退结束）、D2（同额单次入金、7 天期限、固定监督人）、D3（双边日常记录、按案授权、30 天保留）推荐项。此前未答复时的 BLOCKED 记录属于历史，不作为本轮已确认规则。

技术核对来源与只读 RPC 结果见 [生态文档](../ecosystem.md)。USDC 和 Core Mobile 接入的资料核对不替代测试币到账或真机测试。

## 逐项审计

| 要求 | 权威落点 | 核对结论 |
| --- | --- | --- |
| 原始 MVP 与三项 commitment | 001 Spec 第 1、2、8、11 节 | 保留七类页面、两类资金、三名监督人、失联判断；见面和关系确认不增加惩罚；无 AI/DeFi/自建 L1 扩展 |
| D1–D3 与 Q1–Q7 关闭 | 001 Spec 第 14、15 节 | 明确记录用户确认；业务规则无待选项；具体依赖版本和真实测试环境属于后续执行条件 |
| 签署、邀请、监督接受、入金与取消 | 001 Spec 第 3、4、10 节；contracts.md 第 2、3 节 | 五地址权限、摘要、两次入金、7 天截止、原额退出齐全；未全部接受不入金 |
| 时间、回应、申诉、投票、和解 | 001 Spec 第 2、5、10 节 | 截止包含关系统一；及时/迟到不同；无人提交、缺席、未多数均有固定超时出口；旧提议不能复用 |
| 金额与到账 | 001 Spec 第 6、12 节；contracts.md 付款段 | 50/20 示例为 50/90 或 70/70；恢复基金归原主；一次分配、分人支付、失败待付且可重试；最终状态依实际付款 |
| 公共数据和合约接口 | contracts.md 第 1–3 节 | 枚举/单位/对象ID、方法权限、错误和事件已确定；Factory + 单关系实例保留四模块职责；无管理员提款/代签 |
| 链下身份、私有协议、日常记录、证据 | contracts.md 第 4–6 节；architecture.md | SIWE、角色及案件、hash绑定、JSON/附件、幂等和清理齐全；授权不能只靠隐藏 UI；不声称端到端加密 |
| 测试和任务落点 | 001 Spec 第 13 节；Roadmap T005 子任务 | 原 12 项编号保留，扩展至 AC-001-32；全部映射到实现子任务；正常/异常/边界均有输入和结果 |
| 启动工程与钱包基础 | architecture.md；Roadmap T003/T004 | 已有架构、工作区落点、配置字段、钱包连接方法和验证步骤；没有伪造尚不存在的 npm 命令 |

## 验证过程与限制

- 文档静态检查：检查所有本地 Markdown 链接及引用锚点、代码围栏、空白、冲突标记、唯一 AC/任务编号、AC 子任务覆盖、状态表动作与链上方法的对应；检查 Spec/入口为 READY，T002 为 DONE 后再提交。
- 金额走读：R=50/B=20，各退 70+70；A 违约 50+90；B 违约 90+50；只 A 入金时取消退 A=70/B=0；总分配均等于有效入金。重复付款和失败隔离由未来 AC-001-09/24 运行测试证明。
- 边界走读：每个 deadline 时刻普通动作仍有效、timeout 拒绝；deadline+1 反之。说明阶段不能投票；无人提交监督团也可在 voteDeadline+1 结束；迟提交不能延长 voteDeadline。以对应 H 层测试执行为后续完成依据。
- 权限走读：每条资金/状态变更均有角色或“任意地址但固定结果”约束；UI、API 和合约不能各自定义不同结果；链下断连不改变链上托管规则。
- 实际执行：本地 Python 静态审阅脚本与 `git diff --check`，结果由本次提交前输出记录；应用/合约/真机测试未执行，工程尚未初始化。没有任何部署、测试币申请或链上写入。

后续运行条件：T003 锁定依赖；T004 提供 WalletConnect projectId 与 Core 真机；T006 单独授权 Fuji 部署/测试交易并准备测试币。48h/72h 等长时间真实验收不能用缩短时限的测试冒充。

检查中发现：第一轮自动 AC 覆盖检查只识别完整编号，Roadmap 简写导致报告缺项。已将子任务引用展开为完整 AC 编号，保留此失败说明并重新运行检查；未删除任何 AC 或降低行为断言。

最终结果：Python 静态审阅通过 11 份文档、74 个本地链接/锚点、32 个唯一 AC 且全部映射、17 个任务编号、21 个链上业务方法签名和8个生命周期状态；`git diff --check` 退出 0。设计逐项审阅通过，001 Spec 标为 READY，T002 标为 DONE。应用与真实链上验收仍未执行。
