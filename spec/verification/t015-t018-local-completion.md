# T015–T018 本地完成与分层验证

日期：2026-09-19。任务分支：`feat/001-local-completion`；基于 main 的路演 PR #10，保留其文件。工作目录 `/tmp/avalanche-local-completion`。负责人 skelitalynn，AI实施与自检；不是独立队友评审。PR链接待补。GitHub 拒绝工作流推送（OAuth 缺 `workflow` scope），三系统CI未执行。

## 已实现

- 真实界面接入 Radix Themes，LiveApp 拆成独立视图与业务 Hook；与Mock复用品牌主题和纸张插图。保留原Mock业务、断言、合约规则与时间窗口。
- 默认重启恢复原链、回执、加密数据库和密钥；新建会话必须显式 `mvp:new`。操作日志先落盘后执行，重放逐项验证，异常时保留原数据并停止。
- 真实页面支持选择UTC见面日期；钱包变化丢弃旧私有请求结果；待确认交易保存关系地址；证据先核对内容、案件绑定和链上登记再呈现。
- API补协议读取摘要、30天拒读和不存在案件404；固定公开npm registry，提供三系统CI模板；因远程授权限制尚未启用。

## 本地命令证据

使用 Node24.13.0。当前 Linux 实测：18项单元测试、16项合约/API用例、1项持久链恢复测试、1项多场景启动器测试、13项Playwright回归通过；真实五钱包浏览器验收及完整重启复验通过。最终代码及CI状态在交付时补齐。

命令：`contracts:build`、`typecheck`、`typecheck:core`、`npm test`、`test:core`、`test:chain-recovery`、`build:local`、`test:local`、`test:acceptance`、`build`、`test:e2e`。真实浏览器七类页面覆盖1440/390/320px，输出 `artifacts/live`；Mock截图和服务完全独立。

## 32项业务矩阵

H：`contracts/test/core.test.ts` 真本地EVM；A：同文件真实Fastify/SQLite集成；B：`scripts/smoke-live.ts` 真实本地浏览器；V：前台Vitest；P：Mock/服务边界Playwright。表中列出实际覆盖，不能将某行的本地证据扩展为所有设备/网络层都已验收。

| AC-001 | 本地证据及可观察结果 | 真实层 |
| --- | --- | --- |
| 01 | H/A/B 五地址共同协议、摘要核对、越权拒绝 | Fuji待验 |
| 02 | H/B 双签及三接受前拒绝入金，精确双方入金后激活 | Fuji待验 |
| 03 | A/B 单方记录0次，另一方确认后1次；下次确认=完成+7天；代币不变 | 本地覆盖 |
| 04 | H/B A提出不付款，B确认实际70/70 | Fuji待验 |
| 05 | H 无确认、未超时、已及时回应均不可申诉 | 本地覆盖 |
| 06 | H/A/B 超期进入APPEAL，固定说明/投票时间与材料页面 | Fuji待验 |
| 07 | H 越权/重复投票拒绝，一票不分配；B两监督身份操作 | 本地覆盖 |
| 08 | H 两种被申诉方50/90、90/50；B核对50/90链上实际付款 | Fuji待验 |
| 09 | H 终止后重复执行拒绝，无双付 | 本地覆盖 |
| 10 | A/B 未登记/说明期/监督期/越权材料权限 | Fuji待验 |
| 11 | B七类真实页面1440/390/320，P七页Mock；无横向溢出 | Core手机待验 |
| 12 | H余额/授权/回滚边界；B拒签、回执读取中断、刷新后查询同一哈希；V/P错链/服务异常 | Core/Fuji待验 |
| 13 | H零地址、重复、角色重叠及重复接受拒绝 | 本地覆盖 |
| 14 | H不匹配摘要拒绝，无修改接口；A正文篡改409；前台核对摘要 | 本地覆盖 |
| 15 | H邀请/入金截止与+1，接受、存款、expire边界 | 本地覆盖 |
| 16 | H无入金/部分入金/缺监督取消或到期，仅退实存，持续时间0 | Fuji待验 |
| 17 | H非法金额及精确边界0.01/1000、重复入金和fee token回滚；V/P输入精度 | 本地覆盖 |
| 18 | H截止秒回应阻止申诉，迟到回应仍可申诉且不自动付款 | 本地覆盖 |
| 19 | H并发确认拒绝，撤回/过期与旧编号失效 | 本地覆盖 |
| 20 | H固定48+72小时，提前投票/超时拒绝，未升级可超时结束 | 本地覆盖 |
| 21 | H两票否决、0票、1票、1:1超时均70/70 | Fuji待验 |
| 22 | H双方恢复资金不变且activatedAt不变；双方退款结束；旧提议拒绝 | 本地覆盖 |
| 23 | H同一区块两种顺序：结束/申诉、和解/决定票；成功+回滚，仅一次分配 | 本地覆盖 |
| 24 | H拒付代币，A待付/B已付；重试只付A，重入不双付；B显示应得/已付 | 本地覆盖，真实UI拒付演示非本轮路径 |
| 25 | A跨UTC月、双击幂等确认、重复登记；B实际双边见面/关系确认 | 本地覆盖 |
| 26 | A未授权、4001字、伪装图、5MiB+、11条及窗口外拒绝，真实JPEG去EXIF转PNG；H登记上限冻结；B授权上传预览登记 | 本地覆盖 |
| 27 | A终止30天前可读/到期410，停服后启动清理数据库正文附件；链历史保留 | 本地覆盖 |
| 28 | A nonce重放、域/链篡改、过期签名、伪造Header及越权确认拒绝；B五身份独立登录 | 真实钱包切换待验 |
| 29 | A正文/字节/盐篡改及跨案拒绝；V客户端摘要与案件绑定失败不展示；B核对链上登记后查看 | 本地覆盖 |
| 30 | A RPC不可用503、伪造关系404、HTTP幂等；B哈希恢复不重复发送 | 本地覆盖 |
| 31 | H覆盖准备、激活、结束请求、争议、和解、结算待付及终止路径和非法调用 | 本地覆盖，非形式化穷举证明 |
| 32 | A错链/代币/空代码/RPC不可用失败关闭；P错误配置，B缺projectId说明；Mock标识保留 | Core/Fuji待验 |

## 新增恢复与跨平台AC

- AC37：七类真实页面、双确认记录、授权证据和正常/违约资金闭环，B通过；独立Mock语义保持。
- AC38：真实业务快照及私有正文跨完整服务重启逐项相同，重新登录浏览器可读原关系；链测试核对原交易回执、区块hash与余额。
- AC39：尾部intent已落盘但result缺失时恢复一次；篡改日志拒绝且文件不变；强杀父进程释放服务、重启恢复；密钥/DB绑定错误拒绝。
- AC40：默认恢复同一sessionId，显式新建隔离会话且旧目录保留；第二启动器与占用端口拒绝，不改他人服务。
- AC41：工作流模板覆盖Ubuntu/Windows/macOS同一安装、构建、真实EVM/API、恢复和浏览器流程，但GitHub拒绝工作流写入，故仅Linux当前实测，Windows/macOS未执行。真机Core与OS级桌面安装不由手机视口测试代替。

## 发现与修正记录

新增失败用例发现协议GET原先返回已改写正文且缺到期检查，现加入摘要/保留期验证；不存在争议的合约NotFound原先转503，现精确映射404，RPC故障仍503。新增测试预期的时间类型、材料上限曾与冻结接口不一致，已按数字时间戳、每方10条及409修正测试输入/预期，未改变业务规则或删除原断言。

## 外部条件与限制

Fuji只读预检在区块58481121确认43113、官方测试USDC代码和6位精度。未配置WalletConnect projectId，无Fuji部署清单；未提供可操作的Core设备、五个测试账户/资金，且真实48+72小时未经过。没有部署、转账或用加速本地结果冒充真实验收。

恢复实现固定Hardhat/EDR版本，日志重放成本随操作数增长，仅面向本地Demo；原T013无链日志的历史会话不能直接恢复。测试使用公开开发身份/TestUSDC，不涉及真实资产。更换电脑请全新克隆并启动；迁移已有会话需保留整套数据与匹配依赖，不能仅复制SQLite。
