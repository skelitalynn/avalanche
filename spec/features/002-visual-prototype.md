# 002 SituationSHIT 单文件交互原型

状态：READY，仅针对 mock 原型；001 的 Q1–Q7 仍待确认。
关联任务：T014、T015、T016、T017。负责人：本次对话用户，AI 助手实施。

## 需求来源与范围

2026-09-19 用户要求按照最新需求文档改出新的 HTML，随后明确改为连续流程，并要求关系建立后可增减约定、申诉对方违约。原型为中文移动端优先的单文件，不替代 React 正式工程。页面支持通过 EIP-1193 浏览器钱包读取账户和网络，但不请求签名、调用合约或操作真实资金。

## 功能行为

- 首页进入连续流程；依次创建约定、填写两类金额与三位监督人、双方签署、双方模拟入金，再进入关系主页。
- 主页展示当前约定版本与分人分类型资金，支持提出修订、发起申诉、正式确认及双方正常结束。
- 争议页包含确认时间、截止时间、回应和申诉状态，并提供说明、回应、接受结束和提出争议入口。未定路径只提示，不产生结算。
- 监督页只展示必要争议信息，每名好友一票，两票违约后模拟结算；不足两票不转移保证金。
- 正常结束各退本人恢复基金和保证金；B 违约时 A 收到双方保证金，B 只收本人基金。结束后不能再次入金或结算。
- A/B 角色可在签署后切换；不代表身份认证或真实计时。状态只存在内存，刷新重置，不提供虚假的跨设备邀请链接。
- 顶部钱包入口检测 `window.ethereum`，由用户操作后调用 `eth_requestAccounts`；显示缩略地址并以 `eth_chainId` 检查 Fuji（`0xa869`）。错误网络可请求 `wallet_switchEthereumChain`，钱包缺少 Fuji 时使用仓库生态文档中的官方参数请求添加。账户和网络变化即时更新；拒绝、未安装钱包及切换失败均有可理解反馈。
- 钱包连接不绑定 A/B 模拟角色，不请求消息签名或发送交易；签署、存款、投票和结算仍是本地 mock。
- 双方可在建立流程中设置不同称呼，并在关系建立后无待处理事项时再次修改；新称呼按稳定的双方索引同步用于角色切换、签署、资金、申诉、监督和结算，不改变钱包或协议身份。
- 关系结束页先保留模拟结算结果，再提供返回首页入口；确认返回后清空本地关系状态并开始全新流程，钱包连接状态不受影响。
- Q1–Q7 不作业务定案；同额、整笔入金、输入范围、持续时间及完成情况是评审示例。无真实截图，使用 HTML/CSS 插画与 mock 数据。链详情默认折叠。

## 验收标准

## 2026-09-19 连续流程与约定变更

来源：用户要求从“开始一段关系”逐步完成创建、签署，建立后可增减约定并申诉对方违约。此次仅扩展单文件 mock；001 正式链上功能仍为 DRAFT。

- 移除七页自由导航和预装争议场景。按填写约定 → 资金与好友 → 双方签署 → 双方入金 → 关系主页推进；返回编辑保留输入，已签署后不可退回修改协议。越级 hash 导航回到当前合法步骤。
- 顶部步骤条只在关系建立流程中显示；双方完成入金并进入关系主页后隐藏，申诉和结束页面也不再占用该区域。
- 同页使用明确的 A/B 模拟身份切换，每次只能签署、存款、回应或确认属于当前角色的操作；不把模拟身份当成安全认证。
- 初始三条约定可编辑、删除；可添加自定义文字约定，至少保留一条。用户文本按文本安全展示。
- 建立后的增减约定生成修订提案，由发起方确认，再由另一方接受后替换生效并递增版本；拒绝或撤回保留原版。此为本次原型假设，需产品方确认后才能用于真实业务。资金与监督人不能通过约定修订修改。
- 正式确认、申诉或结束请求期间锁定约定编辑。待确认修订期间不能发起新申诉或结束，避免使用未定版本。争议保留发起时的版本与约定快照。
- 申诉可选择当前生效约定、说明情况，自动指定另一人为被申诉方。失联申诉仍需先正式确认并模拟超期且无回应；删除失联约定后不再显示该入口。一般约定可发起 mock 申诉并由对方回应，但裁决标准与资金后果待定，不自动触发投票转账。
- 失联演示可进入三名好友一人一票的监督阶段；两票后按实际被申诉方结算，A/B 对称。正常结束需要双方确认。
- 所有修改仅内存保存，刷新重置；页面提供统一 mock 提示。退出编辑放弃草稿，不改变已生效约定。

新增验收：AC-002-08，顺序推进、返回保留值、不能跳步；AC-002-09，增删修订一方确认不生效、对方接受后版本更新、拒绝保持原版；AC-002-10，选择生效约定发起一般申诉，角色、版本、说明可见，对方可回应且不转账；AC-002-11，失联前置条件、A/B 对称结算及正常结束；AC-002-12，390px 关键流程无横向溢出。原 AC-002-01 的页面入口由连续流程替代，不再要求七页导航。

钱包验收：AC-002-13，无注入钱包时点击连接显示安装提示；AC-002-14，用户批准连接后显示缩略地址及当前网络；AC-002-15，非 Fuji 时可请求切换或添加官方 Fuji 参数，拒绝操作不显示成功；AC-002-16，连接不触发签名或交易，A/B 仍明确标为模拟身份。

称呼验收：AC-002-17，建立流程可填写两个不同的非空称呼；关系建立后可修改并保存，重复称呼被阻止，角色切换、资金和申诉页面立即统一显示新称呼，双方索引与已有状态不变。

结束后验收：AC-002-18，正常结束或模拟裁决结束后先展示资金去向；点击“返回首页，开始新的关系”后进入首页，旧约定、称呼、签署、入金和争议状态被清空，已连接钱包仍保持连接展示。

| 编号 | 操作 | 可观察预期 |
| --- | --- | --- |
| AC-002-01 | 从首页开始并依次操作 | 只能按创建、资金与好友、签署、入金、主页顺序推进 |
| AC-002-02 | 创建、双签、入金 | 同一协议信息可见，未双签存款按钮禁用，资金分类展示 |
| AC-002-03 | 390px 与桌面浏览 | 布局可用，无页面横向溢出 |
| AC-002-04 | 检查标识与技术详情 | mock 标识可见，链信息默认折叠，无真实链调用 |
| AC-002-05 | 发起确认、超期后申诉 | 截止前不能申诉，超期无回应可进入申诉 |
| AC-002-06 | 同一人重复投票、一票、两票违约 | 已投票按钮禁用，一票不结算，两票示例 A90/B50，总计140 |
| AC-002-07 | 单方提出、双方确认结束 | 单方不结算，双方确认示例各70，总计140 |
| AC-002-17 | 建立前设置称呼，建立后再次修改并进入申诉 | 两个不同称呼保存成功并同步到角色、资金和申诉；重复称呼不保存 |
| AC-002-18 | 在结束结果页点击返回首页 | 回到起始页并清空旧关系 mock 状态，钱包状态不重置 |

验证：浏览器人工操作和 DOM 指标、`git diff --check`。此处只验收 mock，不代表 001 的真实功能、隐私权限或 Fuji 验收已通过。
