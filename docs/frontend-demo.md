# SituationSHIT 前台演示

本次交付对应 [002 Spec](../spec/features/002-demo-frontend.md) / T010。七个中文页面、异步 Mock、角色与时间控制已可运行；未接入后端、钱包、合约或真实资金。

当前 React 前台已按仓库根目录 `avalanche-demo.html` 统一视觉：米白背景、墨绿操作色、青柠状态提示、顶部品牌栏、横向关系步骤和抽象双人主视觉。原型只作为视觉与交互基准；七个直达页面、异步 Mock service、持久化、演示控制和验收流程继续由 React 应用实现。

## 启动与截图

推荐 Node 24，最低 22.12；根目录 `npm ci` 后运行 `npm run dev`，打开 <http://localhost:5173>。正式演示建议先 `npm run build`，再 `npm run preview`，减少开发服务器资源占用。两者不能同时占用 5173。

服务器通过 Remote SSH 访问时，在编辑器的端口面板转发 5173，然后从本机浏览器打开上述地址。应用没有远程账户服务，邀请和身份切换只作用于当前浏览器，不支持把本机邀请发给另一台设备。

| 页面 | 直达地址 | 建议展示内容 |
| --- | --- | --- |
| 首页 | `/#/home` | 产品主张和三个核心价值 |
| 创建 | `/#/create` | 规则、两类资金、三位好友 |
| 邀请 | `/#/invite` | 双方完整协议、模拟签署与入金 |
| 关系主页 | `/#/relationship` | 关系进度、约定、基金、好友 |
| 争议 | `/#/dispute` | 正式确认、期限、回应与申诉 |
| 好友监督 | `/#/jury` | 最小必要事实、三人两票 |
| 结束 | `/#/ended` | 50 / 90 或 70 / 70 的资金明细 |

直达地址读取本机当前状态；要查看特定样例，先使用右下角「演示控制」选择场景。选场景会覆盖本机演示存档。截图模式为 `/?present=1#/relationship`，收起控制台并保留模拟数据标识；删除 `present=1` 或点右下角悬浮出现的退出按钮即可恢复控制。

```bash
npx playwright install chromium
npm run screenshots
```

输出 `artifacts/screenshots/`：7 张桌面长图、7 张手机长图、7 张手机首屏图。长图完整展示内容，手机首屏图适合 PPT 中的手机框。长图导出时将底栏移到整页底部，避免导航横穿内容；实际交互仍为固定底栏。截图不会隐藏模拟标识或改变业务数据。

## 三分钟演示

1. **产品与关系主页**：打开首页介绍产品，再选「进行中」场景，展示 42 天、三项约定、每人 50 恢复基金和 20 保证金。
2. **创建和签署**：创建新约定并生成模拟邀请；控制台切为 B，勾选同意并签署。控制台模拟三位好友接受；B 存入 70，再切 A 存入 70，关系生效。昵称、规则、金额会沿用创建输入。
3. **正常结束**：A 提出结束，此时不会结算；切 B 确认，展示双方各 70 USDC。
4. **好友判断**：选择「争议投票」场景，当前为好友 1。投「构成违约」并确认；切好友 2 再投一票，展示 A 50 / B 90。两票否决会各返 70。

如需展示完整失联过程：选择进行中 → 切 B → 确认与申诉 → 发正式确认 → 控制台推进 7 天，再推进 1 小时 → B 发起申诉 → 推进 49 小时 → 提交好友监督 → 分别切好友投票。恰好截止时不能申诉，必须超过截止时间。48 小时说明窗口也不能通过业务按钮跳过。

见面记录与日常关系确认需要双方操作；一方发起后切换另一方确认。刷新会继续本机存档，「重置演示」恢复初始样例。姓名、好友与事件均为虚构；不要输入真实私密证据。

## 后续接入点

- `apps/web/src/data/types.ts`：视图数据、整数微 USDC、异步服务契约。
- `apps/web/src/data/service.ts`：唯一适配入口，当前注入 `createMockService`。
- `apps/web/src/data/mockService.ts`：Mock 状态机、校验、localStorage；测试可注入存储和延迟。
- `App.tsx`：只使用服务快照、订阅、动作，不读取 fixture 或 localStorage。

后端可用后在适配入口增加真实实现，将私有 HTTP 数据和链上权威状态组合成视图，补齐签名、交易 pending/revert、过期取消、和解、附件授权与失败付款重试，再按001验收。控制台角色切换绝不作为真实登录或权限校验。当前 `appeal/voting` 是演示展示子状态，不能直接当作 Solidity 枚举。

MVP 的真实 SIWE、Core、USDC 托管、隐私权限、附件和 Fuji 流程仍由 T003/T004/T005/T006 实施；本任务没有把它们标记为 DONE。演示只保留一段关系、固定起始时钟；未提供跨设备邀请、邀请/存款超时、争议和解及证据上传。

## 验证记录

2026-09-19，在 `/tmp/avalanche-demo-frontend`、`feat/002-demo-frontend` 执行；Node 22.22.0、npm 10.9.4。依赖精确版本见 `package-lock.json`。

| 检查 | 对应 AC | 实际结果 |
| --- | --- | --- |
| `npm ci`、`npm run typecheck`、`npm run build` | AC-002-01 | 均退出 0，页面 HTTP 200 |
| `npm test` | AC-002-03/04/05 | 12 项通过：签署/入金前置条件、精确金额、双边确认、时间边界、两票裁决、重复操作、持久化/坏数据/存储失败 |
| `npm run test:e2e` | AC-002-02 至 AC-002-08 | 最终 8 项浏览器检查通过（28.5 秒）：真实点击创建入金、正常结束、完整失联投票；两种视口七页；无外部请求/页面 JS 错误；截图 |
| 人工视觉检查 | AC-002-06/08 | 首页、关系主页、创建、邀请、监督、结算的桌面/手机图已查看；中文无缺字，关键内容无裁切；投票弹窗有初始焦点、Tab 循环与 Escape 关闭 |
| `git diff --check`、本地文档链接 | 文档规则 | 退出 0；13 份 Markdown、80 个本地文件链接有效 |

T012 视觉适配复验：2026-09-19，在 `feat/002-green-prototype-ui` 执行 `npm run typecheck`、`npm test`、`npm run build`、`npm run test:e2e`，分别通过类型检查、12 项 Vitest、生产构建和 8 项 Playwright。重新导出 21 张 PNG，人工查看桌面/手机首页、创建、关系主页与结算页；确认米白、墨绿、青柠视觉、顶部品牌栏、横向步骤、抽象双人主视觉和业务页面风格一致，390×844 无横向溢出。首次 Playwright 启动因当前版本 Chromium 缺失而失败，安装对应浏览器后未修改断言即全部通过。

首次构建因未使用的 `Role` 类型导入失败，移除后通过。浏览器前两轮在开发服务器约 0.5 GB 内存占用、同机其他服务环境下出现 Chromium 启动/加载超时；第二轮停止开发服务器期间又产生连接拒绝，均未记作通过。原始错误与 trace 保存在本机 `artifacts/verification/first-browser-run`、`second-browser-run`，日志 `browser-rerun.log`；改为生产预览、单 worker、隔离测试 DBus 与关闭 GPU 后 8 项原断言通过（25.4 秒），没有改成模拟浏览器或降低业务断言。最新检查日志与截图不提交到 Git，可按命令复现。

本机浏览器为 Playwright 下载的 Chromium；非真实手机、真实钱包或真实 Fuji 验收。无 CI 通过或独立队友评审的声明。

## 资源

首页主视觉使用 CSS 绘制的抽象双人图形，不加载外部图片。图标使用 `@phosphor-icons/react`（MIT）。字体使用系统中文字体，没有运行时外链字体/图片请求。
