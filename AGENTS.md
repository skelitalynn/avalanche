# 开发规则

适用于本仓库内的人与 AI 编程助手。项目是多人协作的小型 Avalanche Demo，采用轻量 Spec 驱动开发。具体 Git 操作见 [协作指南](docs/collaboration.md)。

## 开始工作

1. 阅读根 [README](README.md)、[Spec 入口](spec/README.md)、[Roadmap](spec/roadmap.md) 和本任务相关的功能 Spec；涉及技术边界时阅读 architecture.md、contracts.md。
2. 检查 `git status --short`、当前分支和已有修改。保留其他人的工作，不覆盖、回滚或提交无关修改。
3. 确认任务负责人、依赖和改动范围；在任务分支工作。不同开发者或并行会话使用各自的 clone/worktree，不在同一工作目录切换分支。
4. 当前仓库仍处文档阶段；基础技术栈已在 [技术架构](spec/architecture.md) 确认，SituationSHIT 产品范围见 [001 Spec](spec/features/001-situationship.md)，实现边界仍待补齐。沿用已确认选型，以真实文件为准，不假设启动、构建、测试命令已经存在。

## 将自然语言转成可执行 Spec

收到“我想要……”“帮我实现……”等需求时，由助手完成下面的整理工作，不要求提出需求的人先手写技术文档。

1. **理解需求**：提取目标用户、问题、输入、操作、结果、范围外事项；保留原始需求摘要和来源（对话摘要或 Issue/PR 链接）。
2. **核对现状**：查看现有 Spec、代码和接口。已有功能优先更新原 Spec，避免创建重复或冲突的定义。
3. **处理歧义**：只追问影响范围、业务结果、权限、数据写入或公共接口的关键缺口。普通实现细节可按现有约定决定，并记录必要假设。已明确和已授权的内容无需反复确认。
4. **写入 Spec**：复制 [功能模板](spec/features/_template.md)，定义正常、异常、边界行为；验收项使用稳定编号 `AC-编号-序号`，包含前置条件、操作和可观察的预期结果。
5. **拆分任务**：在 Roadmap 写入负责人、分支、依赖、实施步骤、对应 AC、验证命令或人工步骤以及完成条件。每项应足够小，能够独立实现和验证。
6. **确认可执行性**：影响实现的歧义已解决，验收标准具体，测试方式可执行后，将功能 Spec 从 DRAFT 改为 READY。用户已明确授权且需求清楚时可以直接进入实现；仅要求分析或设计时交付 Spec 即止。

不要把“支持某功能”“体验良好”“测试通过”作为唯一验收标准。技术栈未确定时可以先写清人工步骤和预期；开始实现前需确定验证工具，自动测试命令随工程初始化补齐，不能把占位命令当成现有能力。

## 实现与变更

- 顺序：Spec → 任务 → 实现 → 验证 → 更新证据。一个小功能的 Spec 和代码可在同一 PR；公共接口涉及多人的并行开发时先对齐接口定义。
- 只实现当前任务范围。行为改变时先更新对应 Spec、受影响 AC 和任务；纯文案、格式或不改变行为的整理可引用现有文档规则，无需新增功能 Spec。
- 接口、数据结构和链上规则变更同步更新 [contracts.md](spec/contracts.md)，模块职责或技术选择变更同步更新 [architecture.md](spec/architecture.md)。
- 保留失败证据，修复实现或说明需求变更理由；不得通过删测试、降低断言或把真实验收改成模拟验收来制造完成结果。
- 新业务规则、接口、链上写入与缺陷修复应有对应的行为验证；低影响文档修改检查链接、内容一致性和格式即可。

## 多人协作与 Git

- `main` 保存经评审的集成结果，日常使用短期任务分支和 PR，不直接推送 main。
- 分支命名：`feat/001-short-name`、`fix/001-short-name`、`docs/short-name`、`chore/short-name`。一个分支围绕一个可评审主题。
- 一项任务有一位负责人；共同实现时拆子任务。认领、编号和公共接口冲突按协作指南处理。
- 提交只包含本任务相关文件；commit 说明变更目的，功能提交关联 Spec/任务编号。不要用 `git add .` 混入他人文件或本地配置。
- 项目负责人已授权默认交付流程：完成已授权的仓库修改后，执行适用检查、提交并推送任务分支、创建 PR，再自行合并到 main，无需逐次询问。用户明确要求只分析、只审查、保留草稿或暂不合并时遵循该次要求。
- 合并前核对远程 PR 的最新提交、可合并状态和适用检查；有 CI 时等待相关检查完成且通过，没有 CI 时如实记录本地验证。按已核对的提交 SHA 合并，不绕过分支保护或必需审批；合并后确认结果并同步本地主线。
- 默认流程允许助手自检后合并，不把自检写成独立队友评审。部署、链上交易及修改远程权限不包含在默认 PR 合并授权中。
- 不强推共享分支或 main，不改写他人历史。遇到冲突先理解双方意图，不能整份选择 ours/theirs 丢弃他人的需求或证据。
- 不自动修改远程权限、分支保护或全局 Git 账号配置。多人机器上按当前任务确认账号，不能仅凭缓存凭据选择身份。

## 测试、验收与交付

- `TODO → DOING → VERIFY → DONE`；阻塞时记录原因与恢复条件。READY 是 Spec 的可实施状态，DONE 是任务的验收状态，两者不混用。
- DONE 需要关联 AC 全部满足并记录实际结果；PR 是否合并单独记录，任务通过验收不代表代码已进入 main。
- 使用仓库实际提供的检查命令。执行范围匹配改动，记录工作目录、命令、退出结果或人工步骤；未执行的检查明确标注。
- 模拟测试、本地集成与真实 Fuji 验收分别报告。涉及链上交易时，哈希仅证明提交，成功需检查回执与业务结果。
- 不把私钥、助记词、访问令牌写入代码、日志、Spec 或 PR。使用示例配置说明字段；网络参数统一引用 [生态文档](spec/ecosystem.md)。
- 交付说明至少包含：改动内容、Spec/任务、验证结果、尚存限制、当前分支及提交/PR 状态。
